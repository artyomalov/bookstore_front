import styled from 'styled-components';

const StyledCatalogHeader = styled.div`
  width: 100%;
  height: 72px;
  display: flex;
  justify-content: space-between;

  .catalog-header__filters {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
`;

export default StyledCatalogHeader;
